{
  "name": "nft-pos-validator-system",
  "version": "1.0.0",
  "description": "NFT Proof of Stake Validator System with Harberger taxes and slashing.",
  "main": "index.js",
  "scripts": {
    "test": "npx hardhat test",
    "deploy": "npx hardhat run scripts/deploy.js --network goerli"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^4.9.3",
    "dotenv": "^16.0.0",
    "ethers": "^6.0.0"
  },
  "devDependencies": {
    "hardhat": "^2.17.0"
  }
}
